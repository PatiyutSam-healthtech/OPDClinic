# คู่มือ: ใช้ระบบนี้เป็น Template ขายให้ลูกค้าคลินิกอื่น

แนวทางที่ใช้: **"1 ลูกค้า = 1 ชุดแยก"** — ก็อปปี้ Google Sheet + Apps Script +
เว็บหน้าบ้าน ให้ลูกค้าแต่ละรายคนละชุด ทำให้ข้อมูลแต่ละคลินิกแยกจากกันเด็ดขาด
โดยธรรมชาติ (Google แยก Sheet/Script/deployment ให้เองอยู่แล้ว) ไม่ต้องเขียนระบบ
แยก tenant เพิ่มเลย

ข้อดี: ใช้โค้ดชุดเดียวกับที่มีอยู่ตอนนี้ได้เลย ไม่ต้องรื้อสถาปัตยกรรม
ข้อจำกัด: ต้องมีคนตั้งระบบให้ลูกค้าใหม่ทุกครั้ง (ไม่ self-service), อัปเดตฟีเจอร์
ใหม่ต้องไปอัปเดตทีละชุดของลูกค้าแต่ละราย (ยังไม่มีระบบอัปเดตรวมศูนย์)

โค้ดในโปรเจกต์นี้ถูกปรับให้ **Sheet เปล่าๆ ใช้งานได้ทันที** แล้ว — ฟังก์ชัน
`getOrCreatePatientsSheet()` และ `getOrCreateSettingsSheet()` ใน `gas/Code.gs`
จะสร้าง sheet ที่จำเป็น (`Patients`, `Settings`) ให้อัตโนมัติพร้อม header/ค่า
default ตอน API/ฟังก์ชันถูกเรียกครั้งแรก เช่นเดียวกับ `OPD_Visits`, `Drugs`,
`ICD10`, `Users`, `Sessions` ที่มีอยู่แล้วก่อนหน้านี้ — **แปลว่าลูกค้าใหม่ไม่ต้อง
สร้าง sheet เองเลยสักตัว**

---

## ขั้นตอนตั้งระบบให้ลูกค้าใหม่ 1 ราย

### 1. เตรียม "ต้นแบบ" (ทำครั้งเดียว)
ทำให้ Apps Script project ปัจจุบันของคุณ (ที่มี `gas/Code.gs` เวอร์ชันล่าสุด และ
`Index.html` เดิม) เป็น **container-bound script** ของ Google Sheet เดียว (คลินิก
ของคุณเองที่ใช้อยู่ตอนนี้ก็ใช้เป็นต้นแบบได้เลย ไม่ต้องแยกไฟล์ใหม่)

### 2. สร้างชุดใหม่ให้ลูกค้า — ใช้ "Make a copy" ของ Sheet
เปิด Google Sheet ต้นแบบ → `File > Make a copy`

**สำคัญ:** การ Make a copy ของ Sheet จะก็อปปี้ **Apps Script ที่ผูกอยู่ (bound
script) ไปด้วยโดยอัตโนมัติ** ได้ Sheet ใหม่ + Script ใหม่ที่แยกจากต้นฉบับ
100% (คนละ Project ID, คนละ deployment ได้) — นี่คือกลไกที่ทำให้แต่ละลูกค้า
แยกข้อมูลกันเด็ดขาดโดยไม่ต้องเขียนโค้ดแยก tenant เพิ่ม

- ตั้งชื่อ Sheet ใหม่ให้เป็นชื่อลูกค้า เช่น "ระบบคลินิก - [ชื่อคลินิกลูกค้า]"
- **ลบข้อมูลตัวอย่าง/ข้อมูลคนไข้จากต้นฉบับออกให้หมด** ก่อนส่งมอบ (แท็บ `Patients`,
  `OPD_Visits`, `Users`, `Sessions` ถ้ามีข้อมูลทดสอบติดมา) — ข้อมูลคนไข้เป็นข้อมูล
  อ่อนไหว ห้ามหลุดข้ามลูกค้า

### 3. Deploy Web App ใหม่สำหรับลูกค้ารายนี้
เปิด Apps Script Editor ของสำเนาใหม่ (Extensions > Apps Script จาก Sheet ที่ก็อปปี้มา)
- `Deploy > New deployment` → Web app → Execute as **Me**, Who has access **Anyone**
- ได้ URL ใหม่เฉพาะของลูกค้ารายนี้ (คนละ URL กับทุกลูกค้าอื่นและกับของคุณเอง)

### 4. ตั้งค่าเฉพาะของลูกค้ารายนี้
- `Project Settings > Script Properties` → เพิ่ม `API_TOKEN` เป็น **ค่าใหม่ที่สุ่มมา
  เฉพาะลูกค้ารายนี้** (ห้ามใช้ค่าเดียวกับลูกค้ารายอื่นหรือของคุณเอง)
- ไปที่แท็บ `Settings` ใน Sheet ใหม่ กรอกชื่อ/ที่อยู่/เบอร์/เวลาทำการของคลินิกลูกค้า
  (แถวที่ 2 คอลัมน์ A-D) — ถ้ายังไม่ได้สร้าง sheet นี้ ระบบจะสร้างให้อัตโนมัติเมื่อ
  เข้าใช้งานครั้งแรก (ค่า default จะขึ้นว่า "คลินิกของฉัน" ให้ไปแก้)
- สร้างบัญชี admin แรกให้ลูกค้า: แก้ `_seedAdminUser()` ใน Code.gs ของสำเนานี้ ใส่
  username/password ที่จะส่งมอบให้ลูกค้า แล้ว Run ครั้งเดียว (ดู `SETUP.md` หัวข้อ 8
  ถ้าลืมขั้นตอน)

### 5. เตรียมหน้าเว็บ (frontend) ของลูกค้ารายนี้
ก็อปปี้ `index.html` ไปเป็นไฟล์/โฟลเดอร์แยกต่างหากสำหรับลูกค้ารายนี้ (เช่น
`customers/ชื่อลูกค้า/index.html` หรือ repo/hosting แยกไปเลยก็ได้) แล้วแก้ 2 บรรทัด
ให้ตรงกับของลูกค้ารายนี้:

```js
const API_URL   = '<Web App URL จากข้อ 3>';
const API_TOKEN = '<API_TOKEN จากข้อ 4>';
```

Deploy ขึ้น hosting (GitHub Pages/อื่นๆ) — จะใช้ subdomain/subpath แยกต่อลูกค้า
หรือ hosting คนละที่เลยก็ได้ แล้วแต่สะดวก

### 6. ทดสอบก่อนส่งมอบ
เปิด URL ของลูกค้า → login ด้วยบัญชี admin ที่สร้างไว้ → ลองลงทะเบียนคนไข้ทดสอบ 1
คน, ตรวจแบบเข้าคิว, ดู dashboard, เข้าเมนู "จัดการผู้ใช้" (ควรเห็นเพราะเป็น admin)
→ ถ้าผ่านหมด **ลบข้อมูลทดสอบทิ้งก่อนส่งมอบจริง**

---

## เช็คลิสต์สรุป (พิมพ์ติดผนังได้เลย)

- [ ] Make a copy Sheet ต้นแบบ → ตั้งชื่อลูกค้า
- [ ] ลบข้อมูลทดสอบ/ตัวอย่างออกจากสำเนาใหม่ให้หมด
- [ ] Deploy Web app ใหม่ (Execute as Me, Anyone) → ได้ URL ใหม่
- [ ] ตั้ง `API_TOKEN` ใหม่ใน Script Properties (ไม่ซ้ำใคร)
- [ ] กรอกข้อมูลคลินิกในแท็บ `Settings`
- [ ] สร้างบัญชี admin แรกผ่าน `_seedAdminUser()`
- [ ] ก็อปปี้ `index.html` แก้ `API_URL`/`API_TOKEN` แล้ว deploy ขึ้น hosting
- [ ] ทดสอบ login + ใช้งานจริงก่อนส่งมอบ
- [ ] ลบข้อมูลทดสอบทิ้งอีกรอบก่อนส่งมอบ

## ข้อควรระวัง

- **ห้ามใช้ API_TOKEN ซ้ำกันข้ามลูกค้าเด็ดขาด** — ถ้าซ้ำ ลูกค้า A จะสามารถยิง API
  ไปโดนข้อมูลคนไข้ของลูกค้า B ได้ทันที (เพราะ token คือกุญแจเดียวที่กันคนนอก)
- ทุกลูกค้าคือ Google Sheet คนละไฟล์ ต้องคิดเรื่อง **เจ้าของไฟล์ (Owner)** ให้ดี —
  ถ้าคุณเป็นเจ้าของ Sheet+Script ของลูกค้าทุกราย (ใช้ Google account เดียวกัน)
  ต้องบริหารจัดการสิทธิ์/quota ของ Apps Script ในบัญชีเดียวเอง (มีข้อจำกัดจำนวน
  Web app ต่อบัญชีในระดับหนึ่ง) ถ้าจะให้ลูกค้าเป็นเจ้าของ Sheet เอง ต้องโอน
  ownership หรือให้ลูกค้าเป็นคน Make a copy เองจากต้นแบบที่คุณแชร์ให้
- อัปเดตโค้ดใหม่ (เช่นแก้บั๊ก, เพิ่มฟีเจอร์) ต้องไปทำซ้ำทีละชุดของลูกค้าแต่ละราย
  ยังไม่มีระบบ auto-update รวมศูนย์ — ถ้าลูกค้าเยอะขึ้นมากควรพิจารณาทำ script
  ช่วย deploy อัตโนมัติ (เช่น Google Apps Script API + clasp) หรือขยับไปสถาปัตยกรรม
  multi-tenant จริงในระยะยาว
